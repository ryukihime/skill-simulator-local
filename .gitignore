# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# --- Prisma & SQLite 追加分 ---

# SQLiteのデータベース実体（バイナリファイル）を除外
dev.db
dev.db-journal
dev.db.backup
backup_dump.sql

# Prismaクライアント（自動生成される型定義など）
# 基本的には node_modules 内に生成されますが、念のため
/src/generated/prisma
node_modules/.prisma
/prisma/*.db

# 環境変数（重要：絶対に上げない）
.env
